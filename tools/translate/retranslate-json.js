(async () => {
    keysToRetranslate = [
        "link.booking",
        "link.tours",
        "link.shop",
        "link.ticket"
    ];
    // parse master json, translate keys from keysToRetranslate and rewrite in target languages
})();